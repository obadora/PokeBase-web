/**
 * 共通の型定義
 */

// Pokemon関連の型定義（今後実装予定）
export type Pokemon = {
  id: number;
  name: string;
  // 詳細は後で追加
};

// Base関連の型定義（今後実装予定）
export type Base = {
  id: string;
  name: string;
  // 詳細は後で追加
};
